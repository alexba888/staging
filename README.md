# staging

Short-lived file storage. Files in this repository are transient — they exist for seconds to minutes while a workflow consumes them, then get removed.

Nothing here is intended for long-term reference.
